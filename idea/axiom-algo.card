公理化算法
===
算法能否[像数学一样](https://us.metamath.org)公理化？
* 尽管有停机定理，不意味着它不可公理化，正如数学有哥德尔不完备一样
* 由于存在变量、指针、数组，不能将算法_完全_归为$\lambda$演算

我的一些想法是：
* 不考虑数据精度，每个基本类型存在一个到$[0,k]\cap\mathbb{N},k\in\mathbb{N}$的一一映射，特别地，实数类型对应实数
* 把数组$a_0,a_1\cdots a_{n-1}$对应一个实数：整数位n，小数点后第np+r位表示$a_r$小数点后第p+1位，定义一套操作api
* 进行指针、引用修改数据时进行一次$\alpha$重命名
* 函数调用进行$\beta$规约

例如
* 函数展开公理
* 覆盖公理 {任意;let x=a;} $\Rightarrow$ x=a
* 不覆盖公理

: by 我
